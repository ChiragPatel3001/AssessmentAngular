import { ClientpipePipe } from './clientpipe.pipe';

describe('ClientpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ClientpipePipe();
    expect(pipe).toBeTruthy();
  });
});

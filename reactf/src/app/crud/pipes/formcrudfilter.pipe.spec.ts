import { FormcrudfilterPipe } from './formcrudfilter.pipe';

describe('FormcrudfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormcrudfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
